// import { fromJS } from 'immutable';
// import { makeSelectTimelineContainerDomain } from '../selectors';

// const selector = makeSelectTimelineContainerDomain();

describe('makeSelectTimelineContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
